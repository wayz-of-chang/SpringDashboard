@rem rem is a comment
@rem @echo off turns off echoing of commands for the entire script
@echo off

@rem %0 keeps track of the directory of the bat file
@rem replace the script name with the name of the script
@rem ruby is used to call ruby scripts
ruby %0\..\ping.rb